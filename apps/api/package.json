{
  "name": "@paperbridge/api",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.ts",
    "start": "node --import tsx src/server.ts",
    "mqtt:probe": "node --import tsx src/main.ts"
  },
  "dependencies": {
    "@paperbridge/protocol": "workspace:*",
    "mqtt": "^5.14.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3"
  }
}
