# Direct printer probe

Optional diagnostic only. This bypasses the ESP32 and is not the Paperbridge
workflow. Use against the simulator or only when explicitly isolating a printer
network issue: `python print_test.py HOST --confirm-direct`.
