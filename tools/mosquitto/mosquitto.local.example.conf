# Copy to ignored mosquitto.local.conf and replace every placeholder.
# Bind only the Mac's wired-LAN address; never use 0.0.0.0 for this MVP.
listener 1883 192.0.2.1
allow_anonymous false
password_file /absolute/path/to/paperbridge-mosquitto.passwd
persistence false
